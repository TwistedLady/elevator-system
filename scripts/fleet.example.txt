# Elevator fleet for the demo seed / simulator.
# One name per line, or comma-separated; '#' starts a comment; blank lines ignored.
# Names are natural-sorted (e1, e2, … e10), so order here doesn't matter.
#
# Copy this to scripts/fleet.txt (picked up automatically by demo-up.sh), or point at
# it explicitly:  FLEET_FILE=scripts/fleet.example.txt scripts/demo-up.sh
# Or pass straight to the console:  elevator-console simulate --elevators-file <path>

e1
e2
e3
e4
