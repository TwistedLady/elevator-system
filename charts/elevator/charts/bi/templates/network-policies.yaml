# Additive NetworkPolicies for BI (a UNION with the parent's base). Separate objects, not edits to
# the base — so no conditionals anywhere: the whole subchart is skipped when bi.enabled is false.
#   - bi <-> bi        : driver <-> executor RPC
#   - bi -> kafka      : executors read elevator-state
#   - bi -> postgres   : read order_status (orders served). The stats read-model is now a Parquet
#                        file on a shared volume, so there is no analytics DB to reach.
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: allow-bi-internal
  labels:
    app.kubernetes.io/managed-by: {{ .Release.Service }}
    app.kubernetes.io/part-of: elevator
    app: elevator-bi
spec:
  podSelector:
    matchLabels: { app: elevator-bi }
  policyTypes: [Ingress]
  ingress:
    - from:
        - podSelector: { matchLabels: { app: elevator-bi } }
---
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: allow-kafka-from-bi
  labels:
    app.kubernetes.io/managed-by: {{ .Release.Service }}
    app.kubernetes.io/part-of: elevator
    app: elevator-bi
spec:
  podSelector:
    matchLabels: { app: kafka }
  policyTypes: [Ingress]
  ingress:
    - from:
        - podSelector: { matchLabels: { app: elevator-bi } }
      ports:
        - { protocol: TCP, port: 9092 }
---
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: allow-postgres-from-bi
  labels:
    app.kubernetes.io/managed-by: {{ .Release.Service }}
    app.kubernetes.io/part-of: elevator
    app: elevator-bi
spec:
  podSelector:
    matchLabels: { app: postgres }
  policyTypes: [Ingress]
  ingress:
    - from:
        - podSelector: { matchLabels: { app: elevator-bi } }
      ports:
        - { protocol: TCP, port: 5432 }
