# RBAC for Pekko Cluster Bootstrap's kubernetes-api discovery: app pods LIST pods
# (label app=elevator-app) to find each other and form one cluster.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: elevator-app
  labels:
    app.kubernetes.io/managed-by: {{ .Release.Service }}
    app.kubernetes.io/part-of: elevator
---
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: elevator-app-pod-reader
  labels:
    app.kubernetes.io/managed-by: {{ .Release.Service }}
    app.kubernetes.io/part-of: elevator
rules:
  - apiGroups: [""]
    resources: ["pods"]
    verbs: ["get", "list", "watch"]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: elevator-app-pod-reader
  labels:
    app.kubernetes.io/managed-by: {{ .Release.Service }}
    app.kubernetes.io/part-of: elevator
subjects:
  - kind: ServiceAccount
    name: elevator-app
roleRef:
  kind: Role
  name: elevator-app-pod-reader
  apiGroup: rbac.authorization.k8s.io
