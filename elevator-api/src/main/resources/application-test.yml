# TEST profile: verbose, human-readable colored console (Spring Boot's default format).
logging:
  level:
    pl.feelcodes.elevator: TRACE   # our own code at full detail
    org.springframework: DEBUG     # framework internals (requests, beans, kafka listener)
